# Canonical site URL used for metadata (Open Graph, canonical URLs).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Set automatically in the GitHub Pages deploy workflow. Leave unset for local
# development so the app is served from the site root.
# PAGES_BASE_PATH=/rean-linux
